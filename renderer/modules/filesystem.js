'use strict';

class Filesystem {

	/**
	 * Creates workdir foler inside temp folder
	 * @return {[type]}
	 */
    createWorkdir() {}

    /**
     * Copies template from templates folder to workdir
     * @return {[type]}
     */
    copyTemplate() {}
}

module.exports = new Filesystem;