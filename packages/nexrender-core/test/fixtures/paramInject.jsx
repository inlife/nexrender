/*
TODO: should not pick up params in comments
*/
// TODO: should not pick up params in comments
// TODO: should/does it pick up params in quoted strings?

const a = NX.get('unittest_undefined_parameter')
const b = NX.get('unittest_defined_parameter')

const result = NX.call('onePlusOne', 1)